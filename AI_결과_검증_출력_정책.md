# AI 결과 검증·출력 정책

## 왜 검증하는가

AI API가 JSON 형식을 따르도록 요청해도, 내용까지 미션 규칙을 만족한다는 보장은 없다. 따라서 API 응답을 바로 출력하거나 Git 명령에 넘기지 않고, `validation.py`에서 먼저 `CommitDraft` 또는 `PRDraft`로 변환한다.

## 검증 규칙

- `title`은 비어 있지 않은 문자열이어야 한다. 여러 줄 또는 연속 공백은 한 칸으로 정리한다.
- Commit 제목은 최대 72자, PR 제목은 최대 80자다.
- 제목이 제한을 넘으면 자동으로 자르지 않고 오류를 보여 준다. 기계적인 자르기는 의미·동사·대상 일부를 잃을 수 있기 때문이다. 사용자는 더 짧은 프롬프트 결과를 받거나 제목을 직접 고친다.
- 본문과 PR 섹션 값은 문자열 배열이어야 한다. 빈 문자열 및 불릿만 있는 항목은 제거한다.
- 모델이 이미 `- 항목`, `* 항목`, `• 항목`처럼 불릿을 넣었더라도 제거한 뒤 출력 모듈이 불릿을 정확히 한 번만 붙인다.
- PR의 `why`, `what`, `how_to_test`에는 각각 비어 있지 않은 항목이 최소 1개 필요하다.

## 출력 규칙

- Commit에는 제목과 불릿 본문을, PR에는 `## Why`, `## What`, `## How to Test` 세 섹션을 출력한다.
- 출력의 위아래 구분선과 마지막 안내 문구는 AI가 작성한 내용이 아니라 터미널 UX용이다.
- 모든 초안에는 실제 변경 사항과 테스트 방법을 사용자가 최종 검토해야 한다는 안내를 표시한다.
