# AI Git Helper CLI 제출 증빙

이 문서는 구현 결과를 평가 항목별로 찾기 쉽게 연결한 자료다. 실제 Gemini API 호출 결과를 증빙으로 사용하지 않았으며, Key·개인정보·실제 diff를 포함하지 않는다.

| 확인 항목 | 구현 근거 | 자동 검증 근거 |
| --- | --- | --- |
| 터미널 `commit`·`pr` 명령 | `main.py`, `ai_git_helper/cli.py` | `tests/test_cli_integration.py` |
| Git status·staged/unstaged diff 수집 | `ai_git_helper/git_service.py` | `tests/test_scaffold.py` |
| 변경 없음일 때 API 미호출 | `cli.py`의 `changes.has_changes` 분기 | `test_no_changes_exits_before_api_key_or_api_call` |
| API Key 환경변수 사용 및 미출력 | `ai_git_helper/api_client.py`, `.env.example`, `.gitignore` | `tests/test_api_client.py` |
| 안전한 diff 전송 | `ai_git_helper/safety.py`, `ai_git_helper/prompts.py` | `tests/test_safety.py`, `tests/test_prompts.py` |
| Commit·PR JSON Schema와 프롬프트 | `ai_git_helper/prompts.py` | `tests/test_prompts.py` |
| API 1회 호출과 오류 처리 | `ai_git_helper/api_client.py`, `ai_git_helper/cli.py` | `tests/test_api_client.py`, `tests/test_cli_integration.py` |
| 제목·필수 섹션 검증과 출력 | `ai_git_helper/validation.py`, `ai_git_helper/output.py` | `tests/test_validation_and_output.py` |
| 실제 Git을 포함한 통합 흐름 | `cli.py` | `test_real_temporary_git_repository_flows_to_cli_output` |

## 재현 절차

```bash
source .venv/bin/activate
python -m unittest discover -s tests -v
python -m compileall -q .
python main.py --help
```

실제 AI API를 호출하지 않고도 `--show-prompt`로 Git 수집과 safe-mode 이후의 프롬프트를 확인할 수 있다.

```bash
python main.py commit --show-prompt
```

실제 API 호출은 사용자가 Key를 셸 환경변수로 설정한 경우에만 수행하며, 생성물은 초안으로서 사람이 검토해야 한다.
