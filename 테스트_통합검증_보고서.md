# Phase 08 테스트·통합 검증 보고서

## 자동 테스트 범위

| 미션 핵심 기능 | 검증 위치 |
| --- | --- |
| Git status 파싱, staged·unstaged diff, Git 저장소 오류 | `tests/test_scaffold.py` |
| 변경 없음 시 API 미호출, CLI 종료 코드 | `tests/test_cli_integration.py` |
| safe-mode 마스킹 및 줄·문자 제한 | `tests/test_safety.py` |
| Git 컨텍스트, Commit·PR 프롬프트와 JSON Schema | `tests/test_prompts.py` |
| API Key 누락, 성공 응답, timeout, 인증, 429, 5xx, JSON 오류 | `tests/test_api_client.py` |
| Commit·PR 데이터 검증과 터미널 출력 | `tests/test_validation_and_output.py` |
| commit/pr API 1회 호출, 오류 메시지, Ctrl+C, 실제 임시 Git 저장소 통합 흐름 | `tests/test_cli_integration.py` |

## 테스트 격리 원칙

- HTTP 요청은 `requests.post` 또는 `GeminiClient.generate_json` mock으로 바꾼다. 실제 API Key와 외부 네트워크를 사용하지 않는다.
- Git 관련 테스트는 `TemporaryDirectory`에 별도 저장소를 만들며, 전역 Git 설정을 바꾸지 않는다.
- CLI 통합 테스트가 현재 디렉터리를 바꿀 때는 `finally`에서 원래 작업 디렉터리로 복구한다.
- 환경변수 테스트는 `patch.dict(..., clear=True)`로 테스트가 끝난 뒤 원래 환경을 보존한다.

## 실행 명령

```bash
source .venv/bin/activate
python -m unittest discover -s tests -v
python -m compileall -q .
```

## 수동 확인이 필요한 항목

자동 테스트는 API 요청 형식과 오류 처리를 mock으로 확인한다. 실제 Gemini API Key를 사용한 호출은 비용·쿼터·네트워크 영향을 가지므로, 제출 전 사용자가 다음을 직접 확인한다.

1. `GEMINI_API_KEY`를 셸 환경변수로만 설정한다. `.env`나 소스 코드에 저장하지 않는다.
2. 변경 사항이 있는 Git 저장소에서 `python main.py commit` 또는 `python main.py pr`를 실행한다.
3. 출력 초안의 제목 길이, 변경 사실, `How to Test` 내용을 사람이 최종 검토한다.
4. safe-mode가 모든 민감정보를 탐지한다는 보장은 없으므로, 외부 전송 전 `--show-prompt` 결과를 확인한다.
